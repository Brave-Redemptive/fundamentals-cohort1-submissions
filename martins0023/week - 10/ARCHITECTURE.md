# SyncForge - Visual Reference & Architecture Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         BROWSER                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                 React Application                     │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │            App.tsx (Root)                       │  │  │
│  │  │  ┌──────────────┬──────────────────────────────┐ │  │
│  │  │  │ ProjectsPage │     TasksPage (Kanban)       │ │  │
│  │  │  ├──────────────┼──────────────────────────────┤ │  │
│  │  │  │ • Grid View  │ • 4 Columns               │ │  │
│  │  │  │ • CRUD       │ • Drag & Drop             │ │  │
│  │  │  │ • Forms      │ • Filtering               │ │  │
│  │  │  └──────────────┴──────────────────────────────┘ │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │                        ↓                                  │  │
│  │              api.ts (Axios Client)                       │  │
│  └───────────────────────────────────────────────────────┘  │
│                        ↓ HTTP                                 │
└─────────────────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        │       (CORS-enabled)          │
        │               │               │
        ↓               ↓               ↓
    ┌───────────────────────────────────────┐
    │      Backend Server (Port 5000)       │
    │  ┌─────────────────────────────────┐  │
    │  │  Express.js + TypeScript        │  │
    │  │  ┌─────────────────────────────┐│  │
    │  │  │ Middleware                  ││  │
    │  │  │ • CORS                      ││  │
    │  │  │ • JSON Parser               ││  │
    │  │  │ • Logging                   ││  │
    │  │  │ • Error Handler             ││  │
    │  │  └─────────────────────────────┘│  │
    │  │                                 │  │
    │  │ ┌─────────────────────────────┐ │  │
    │  │ │ Routes                      │ │  │
    │  │ │ • /api/projects             │ │  │
    │  │ │ • /api/tasks                │ │  │
    │  │ │ • /health                   │ │  │
    │  │ └─────────────────────────────┘ │  │
    │  │                                 │  │
    │  │ ┌──────────┬──────────────────┐ │  │
    │  │ │ Services │ Middleware       │ │  │
    │  │ ├──────────┼──────────────────┤ │  │
    │  │ │ Project  │ • Validation     │ │  │
    │  │ │ Task     │ • Error Handler  │ │  │
    │  │ └──────────┴──────────────────┘ │  │
    │  │                                 │  │
    │  │ ┌─────────────────────────────┐ │  │
    │  │ │ In-Memory Storage           │ │  │
    │  │ │ • Projects Map              │ │  │
    │  │ │ • Tasks Map                 │ │  │
    │  │ └─────────────────────────────┘ │  │
    │  └─────────────────────────────────┘  │
    └───────────────────────────────────────┘
```

---

## 📱 Frontend Component Hierarchy

```
App
├── Header (Navigation + Branding)
├── Nav (Page Navigation)
├── Main
│   ├── ProjectsPage
│   │   ├── Header (Title + New Button)
│   │   ├── ProjectForm (Conditional)
│   │   └── ProjectsGrid
│   │       └── ProjectCard[] (Map each project)
│   │           ├── Header (Name + Badge)
│   │           ├── Description
│   │           ├── Meta (Team size)
│   │           └── Actions (View, Delete)
│   │
│   └── TasksPage
│       ├── Header (Back button + Title)
│       ├── TaskForm (Conditional)
│       ├── Filters (Status buttons)
│       └── TasksContainer (Kanban grid)
│           ├── TaskColumn[] x4 (To Do, Progress, Review, Done)
│           │   ├── ColumnHeader
│           │   └── TaskList
│           │       └── TaskCard[]
│           │           ├── Title
│           │           ├── Description
│           │           ├── Meta (Assignee, Priority)
│           │           └── Actions (Status, Delete)
│           │
└── Footer
```

---

## 🔄 Data Flow Diagram

### Create Project Flow
```
User Input (Form)
      ↓
ProjectsPage.tsx (State Management)
      ↓
api.ts (projectAPI.create())
      ↓
HTTP POST /api/projects
      ↓
Express Routes (projects.ts)
      ↓
Validation
      ↓
Service Layer (projectService.ts)
      ↓
In-Memory Storage
      ↓
Response JSON
      ↓
api.ts (Parse Response)
      ↓
ProjectsPage.tsx (Update State)
      ↓
UI Re-render with new Project
```

### Update Task Status Flow
```
User clicks Status Dropdown
      ↓
TasksPage.tsx (handleUpdateTask)
      ↓
api.ts (taskAPI.update(id, {status}))
      ↓
HTTP PUT /api/tasks/:id
      ↓
Express Routes (tasks.ts)
      ↓
Service Layer (taskService.ts)
      ↓
In-Memory Storage (Update)
      ↓
Response JSON with updated task
      ↓
TasksPage.tsx (Update state)
      ↓
Task Card moves to new column
```

---

## 📊 API Request/Response Cycle

```
Frontend                          Backend
┌────────────────┐               ┌──────────────┐
│ TasksPage.tsx  │               │ server.ts    │
└────────────────┘               └──────────────┘
       │                                │
       │ 1. User fills form             │
       │ 2. Click submit                │
       ├─────────POST /api/tasks────────>
       │    (JSON body)                 │
       │                          3. Receive
       │                          4. Parse JSON
       │                          5. Validate
       │                                │
       │                    ┌───────────┴────┐
       │                    │                │
       │                    ↓                │
       │            6. Check validation    │
       │                    │                │
       │                    ├─ Pass ────────┐
       │                    └─ Fail ───────┐│
       │                                  ││
       │                             7a. Error Response
       │                    ┌─────────────┐ ││
       │                    │ Return 400  │ ││
       │                    │ Error msg   │ ││
       │                    └─────────────┘ ││
       │                                    ││
       │            7b. Store in Map       │
       │                ├─────────────────┐│
       │                │ Create UUID     ││
       │                │ Add timestamp   ││
       │                │ Set status      ││
       │                │ Store in Map    ││
       │                └─────────────────┘│
       │                                    │
       │            8. Return Response     │
       │    ┌──────────────────────────────┐
       │<──────201 + JSON Task──────────────
       │
       │ 9. Parse response
       │ 10. Update component state
       │ 11. Re-render with new task
       │
       └─ UI shows new task in grid
```

---

## 🗄️ Data Model Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                      In-Memory Storage                       │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Projects Map:                                               │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ "proj-123" → {                                         │ │
│  │   id: "proj-123",                                      │ │
│  │   name: "Website Redesign",                            │ │
│  │   description: "...",                                  │ │
│  │   teamSize: 5,                                         │ │
│  │   status: "active"                                     │ │
│  │ }                                                       │ │
│  └────────────────────────────────────────────────────────┘ │
│                           ↑                                   │
│                    (1 to Many)                               │
│                           ↓                                   │
│  Tasks Map:                                                  │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ "task-456" → {                                         │ │
│  │   id: "task-456",                                      │ │
│  │   projectId: "proj-123", ───┐ (Foreign Key)            │ │
│  │   title: "Homepage",         │                          │ │
│  │   description: "...",        │                          │ │
│  │   status: "in-progress",     │                          │ │
│  │   priority: "high",          │                          │ │
│  │   assignee: "john@..."       │                          │ │
│  │ }                            │                          │ │
│  │                              │                          │ │
│  │ "task-789" → {               │                          │ │
│  │   id: "task-789",            │                          │ │
│  │   projectId: "proj-123" ─────┘                          │ │
│  │   ...                                                    │ │
│  │ }                                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 UI Layout Wireframes

### Projects Page
```
┌──────────────────────────────────────────────────────────┐
│ 🔄 SyncForge - Distributed Task Management              │
├──────────────────────────────────────────────────────────┤
│ 📁 Projects      ✓ Tasks                                  │
├──────────────────────────────────────────────────────────┤
│ 📁 Projects                              [➕ New Project]  │
│                                                            │
│ ┌──────────────┬──────────────┬──────────────┐           │
│ │ Project Card │ Project Card │ Project Card │           │
│ │ ├─ Name      │ ├─ Name      │ ├─ Name      │           │
│ │ ├─ Desc      │ ├─ Desc      │ ├─ Desc      │           │
│ │ ├─ 👥 5      │ ├─ 👥 3      │ ├─ 👥 4      │           │
│ │ ├─ [View]    │ ├─ [View]    │ ├─ [View]    │           │
│ │ └─ [Delete]  │ └─ [Delete]  │ └─ [Delete]  │           │
│ └──────────────┴──────────────┴──────────────┘           │
│                                                            │
│ ┌──────────────┬──────────────┐                           │
│ │ Project Card │ Project Card │                           │
│ │ ...          │ ...          │                           │
│ └──────────────┴──────────────┘                           │
└──────────────────────────────────────────────────────────┘
```

### Tasks Page (Kanban)
```
┌──────────────────────────────────────────────────────────┐
│ 🔄 SyncForge - Distributed Task Management              │
├──────────────────────────────────────────────────────────┤
│ [← Back] ✓ Tasks                       [➕ New Task]     │
├──────────────────────────────────────────────────────────┤
│ [All] [To Do (3)] [In Progress (2)] [Review (1)] [Done (4)]
│                                                            │
│ ┌─────────────┬───────────────┬────────────┬────────────┐│
│ │ To Do       │ In Progress   │ Review     │ Done       ││
│ ├─────────────┼───────────────┼────────────┼────────────┤│
│ │ ┌─────────┐ │ ┌───────────┐ │ ┌────────┐ │ ┌────────┐ ││
│ │ │Task 1   │ │ │Task 3     │ │ │Task 5  │ │ │Task 7  │ ││
│ │ │Homepage │ │ │API Setup  │ │ │Deploy  │ │ │Release │ ││
│ │ │High ⚙   │ │ │Medium ⚙   │ │ │High ⚙  │ │ │Low ⚙   │ ││
│ │ │👤 John  │ │ │👤 Jane    │ │ │👤 Bob  │ │ │👤 none │ ││
│ │ │[✓ ▾]    │ │ │[✓ ▾]      │ │ │[✓ ▾]   │ │ │[✓ ▾]   │ ││
│ │ └─────────┘ │ │           │ │ │        │ │ │        │ ││
│ │             │ │           │ │ │        │ │ │        │ ││
│ │ ┌─────────┐ │ │ ┌───────┐ │ │ └────────┘ │ └────────┘ ││
│ │ │Task 2   │ │ │ │Task 4 │ │ │           │            ││
│ │ │Database │ │ │ │Tests  │ │ │           │            ││
│ │ │Medium ⚙ │ │ │ │Low ⚙  │ │ │           │            ││
│ │ │👤 none  │ │ │ │👤 Bob │ │ │           │            ││
│ │ │[✓ ▾]    │ │ │ │[✓ ▾]  │ │ │           │            ││
│ │ └─────────┘ │ │ └───────┘ │ │           │            ││
│ └─────────────┴───────────────┴────────────┴────────────┘│
└──────────────────────────────────────────────────────────┘
```

---

## 🔌 API Endpoint Summary

```
┌──────────────────────────────────────────────────────────┐
│            API Endpoint Organization                      │
├──────────────────────────────────────────────────────────┤
│                                                            │
│ Health & Status:                                           │
│   GET  /health                  → Server status           │
│                                                            │
│ Projects:                                                  │
│   GET    /api/projects          → List all                │
│   POST   /api/projects          → Create                  │
│   GET    /api/projects/:id      → Get one                 │
│   PUT    /api/projects/:id      → Update                  │
│   DELETE /api/projects/:id      → Delete                  │
│   GET    /api/projects/status/  → Filter by status        │
│                                                            │
│ Tasks:                                                     │
│   GET    /api/tasks             → List (paginated)        │
│   POST   /api/tasks             → Create                  │
│   GET    /api/tasks/:id         → Get one                 │
│   PUT    /api/tasks/:id         → Update                  │
│   DELETE /api/tasks/:id         → Delete                  │
│   GET    /api/tasks/status/     → Filter by status        │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

---

## 📋 Development Workflow

```
┌──────────────────────────────────────────────────────────┐
│              Weekly Development Cycle                     │
├──────────────────────────────────────────────────────────┤
│                                                            │
│ MONDAY:  Project Planning                                │
│   • Create GitHub Issues                                  │
│   • Plan sprints                                          │
│   • Assign issues                                         │
│                                                            │
│ TUESDAY-THURSDAY: Development                            │
│   • Create feature branches                               │
│   • Write code                                            │
│   • Commit with clear messages                            │
│   • Open PRs with descriptions                            │
│   • Peer review                                           │
│                                                            │
│ FRIDAY: Quality & Submission                             │
│   • Run quality checks (lint, test, build)               │
│   • Final code reviews                                    │
│   • Merge to main                                         │
│   • Submit project                                        │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

---

## 🔀 Git Branching Model

```
main ───────────────●──────────────●─────────●
       (production) ▲              ▲         ▲
                    │              │         │
                  merge          merge     merge
                    │              │         │
develop ───────────●──────────────●─────────●──────●
          (integration)            │         │      │
                                   │         │      │
feature/               feature/   │         │      │
auth ───────────────────●─────────┘         │      │
                     (PR review)            │      │
                                            │      │
feature/              feature/              │      │
export─────────────────●────────────────────┘      │
                    (PR review)                     │
                                                    │
bugfix/               bugfix/                       │
cors───────────────────●───────────────────────────┘
                    (PR review)

Legend:
─── Branch line
● Commit
──●── Merge point
```

---

## 🚀 Deployment Pipeline

```
┌──────────────────────────────────────────────────────────┐
│              CI/CD Pipeline Flow                          │
├──────────────────────────────────────────────────────────┤
│                                                            │
│ Developer pushes code                                     │
│         ↓                                                  │
│ GitHub triggers workflow                                 │
│         ↓                                                  │
│ ┌─────────────────────────────────────────────────────┐  │
│ │ Stage 1: Install Dependencies                      │  │
│ │ - npm ci                                            │  │
│ └─────────────────────────────────────────────────────┘  │
│         ↓                                                  │
│ ┌─────────────────────────────────────────────────────┐  │
│ │ Stage 2: Quality Checks                             │  │
│ │ - ESLint (0 errors required)                        │  │
│ │ - TypeScript (strict mode)                          │  │
│ │ - Type-check                                        │  │
│ └─────────────────────────────────────────────────────┘  │
│         ↓                                                  │
│ ┌─────────────────────────────────────────────────────┐  │
│ │ Stage 3: Build                                      │  │
│ │ - npm run build                                     │  │
│ │ - Generate dist/                                   │  │
│ └─────────────────────────────────────────────────────┘  │
│         ↓                                                  │
│ ┌─────────────────────────────────────────────────────┐  │
│ │ Stage 4: Tests & Coverage                           │  │
│ │ - npm test                                          │  │
│ │ - Code coverage report                              │  │
│ └─────────────────────────────────────────────────────┘  │
│         ↓                                                  │
│    ✅ All passed?                                        │
│    ├─ Yes → ✅ Build successful                          │
│    │      Artifacts created                              │
│    │      Ready to merge                                 │
│    │                                                      │
│    └─ No → ❌ Build failed                               │
│           Check logs                                     │
│           Fix issues                                     │
│           Push again                                     │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

---

## 📊 Metrics & Standards

```
┌──────────────────────────────────────────────────────────┐
│          Code Quality Standards                           │
├──────────────────────────────────────────────────────────┤
│                                                            │
│ ESLint:              0 errors     ✅ REQUIRED            │
│ TypeScript:          Strict mode  ✅ REQUIRED            │
│ Test Coverage:       > 70%        ✅ TARGET             │
│ Bundle Size:         < 500KB      ✅ TARGET             │
│ API Response Time:   < 500ms      ✅ TARGET             │
│ Page Load Time:      < 2 seconds  ✅ TARGET             │
│ Type Coverage:       100%         ✅ TARGET             │
│ Comments:            Well-doc     ✅ REQUIRED            │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

---

This completes the comprehensive SyncForge implementation guide! 🎉