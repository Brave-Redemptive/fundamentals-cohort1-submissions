# WaveCom Notifier — Frontend (TypeScript + Vite + React)

## Setup

1. Install deps
   npm install

2. Set backend base URL

   - Option A: set env var when running:
     VITE_API_BASE=http://localhost:4000 npm run dev
   - Option B: create .env in project root with:
     VITE_API_BASE=http://localhost:4000

3. Start dev server
   npm run dev
   Visit http://localhost:5173
