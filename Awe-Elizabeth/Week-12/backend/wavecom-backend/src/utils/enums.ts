export enum notificationChannels{
    sms = "sms",
    email = "email",
    push = "push"
}

export enum notificationStatus{
    queued = "queued",
    processing = "processing",
    success = "success",
    failed = "failed"
}
